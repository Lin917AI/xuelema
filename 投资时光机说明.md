# 投资时光机 · 个人使用版（v3.1）

在原有“学了么”首页增加独立工具入口，原有 164 篇文章和阅读功能不变。

## 已实现

- 纳斯达克100、标普500、黄金；人民币（计入汇率）或美元口径。
- 2000 年 1 月起，按月份自由选择开始与结束；也可选择数据包的最后完整月份。
- 初始资金、月投金额、模拟年费率；暂停/继续、播放速度、跳到下一历史节点。
- 8 个精选重大事件节点，月末暂停，调整从次月生效；设为 0 只停止新增投入，不出售已有份额。
- 资产与本金曲线、盈亏、XIRR、单位净值最大回撤、前高恢复周期、原计划与调整计划对照、同现金流的三资产比较。
- 年度价格收益表、最近十年对比图、基金快照的名称/代码/指数/申购状态筛选。
- 个人 JSON 导入、格式与数据校验、IndexedDB 保存、备份导出。全部计算与数据处理在本浏览器完成，没有数据上传接口。
- 移动端、键盘与原生对话框、浅色/深色样式、减少动画偏好、已缓存后的离线使用。

## 首次在手机使用

1. 把电脑中项目根目录 `个人数据/投资时光机-个人数据.json` 保存到手机“文件”。可由用户自行选择隔空投送或 iCloud 等方式。
2. 在平时使用的“学了么”主屏幕 App 内，打开“投资时光机”，点击“导入个人数据包”并选中 JSON。
3. 显示“已保存在此设备”后，同一入口重新打开通常不必重复导入。不同浏览器或主屏幕 App 可能隔离存储；无痕、存储回收、清理网站数据或换设备后需重新导入，务必保留备份。

本次数据完整至 2026-08。基金快照检查日为 2026-09-16，不是实时申购状态；点击名称到天天基金核对最新情况。

## 数据口径（重要）

| 数据 | 来源 | 口径 |
| --- | --- | --- |
| 纳斯达克100 | Nasdaq，经 FRED NASDAQ100 | 每日收盘价格指数的月均，不含股息再投资 |
| 标普500 | Robert J. Shiller，官方 Data 表 P 列 | 每日收盘价格的月均，不含股息再投资 |
| 黄金 | World Bank Pink Sheet，Gold | 美元/金衡盎司月均 |
| 汇率 | Federal Reserve，经 FRED EXCHUS | 人民币元/1美元月均 |
| 基金观察 | 天天基金公开净值 JS 与费率 HTML | 原渠道的净值、申购状态、限额与费用快照 |

所有系列使用共同完整月份，无插值、缺月填补或当前未完成月。1999-12 仅作首月基准。

每月按上月均价买入，以本月均价估值。这是统一月频的教学近似，不是可按该价格实际成交的回测，也不是常见月末收盘价回测。人民币结果使用各月对应的汇率；额外模拟费率默认 0。

年度变化为本年最后可用参考价除以上年12月参考价减1。它不是定投收益。股票价格指数不含股息，黄金没有股息，因此三资产对比不是总回报的完全公平比较。月度回撤遗漏月内低点。

XIRR 按每次月初入金和最后一个月末资产，使用真实日期与365天年化。不能把增加本金后的资产上升当作策略改善。短期年化可能很大，不意味着可重复获得。

个人版不提供自动每日更新。历史数据来源的公开下载，不代表允许完整数据公开再分发；因此行情与基金快照不进入仓库、GitHub Pages、Service Worker 或公共 CI。数据包保留来源及权利声明。只发布代码、原创事件文字与来源链接。

## 本地维护

目录保持为：

```text
学了么/
  个人数据/                # 私有；不复制到任何网站
  网站文件/                # 原静态网站 / GitHub 仓库的根
    invest/                # 页面、样式、计算、本地存储、事件
    scripts/               # 仅在本机手动生成个人数据
    tests/                 # 全部采用合成数据，不含私有行情
```

使用 Python 3.10+，在本机虚拟环境安装 `scripts/requirements-personal-data.txt`。运行：

```sh
python 网站文件/scripts/build-personal-data.py
```

脚本仅接受“网站文件/scripts”目录结构；拒绝向网站文件目录或项目外输出。生成前验证全部源，输出到项目的“个人数据”，权限600，回读校验后原子替换。源站拒绝或格式异常会停止，不绕过验证、不用虚假数据填充。没有定时任务，也不自动部署个人数据。

之后用户把新包重新导入手机；旧包的真实截止日期会一直保留到导入成功。格式校验失败不会替换原有本地数据。

## 验证命令

这是原生静态网站，没有打包构建步骤。Node 22+ 可运行：

```sh
node --test 网站文件/tests/*.test.cjs
python3 -m http.server 8773 --bind 127.0.0.1 --directory 网站文件
```

在 GitHub 仓库根目录运行时去掉 `网站文件/` 前缀。不得把个人数据目录复制进网站以便测试。

46 项自动测试覆盖 288 组四年情景的逐笔独立计算、暂停恢复、币种、费用、XIRR、回撤、日期边界、事件顺序与文章链接、导入校验、隐私网络检查以及 PWA 分页缓存和跨项目缓存保护。新增 UI 回归测试用合成行情和可控时钟验证220ms / 44ms两档计时、八节点遍历不重复、键盘与触屏焦点区别，以及移除快捷区间后保留自由日期选择。

浏览器验收包括：320px、390px 手机视口与1200px桌面；从2000年开始；2003—2013完整结束；2012—2020黄金美元口径并计费；事件次月停投/恢复；导入后重新打开；错误日期和错误文件不破坏原数据；年度表和基金筛选；无整页横向溢出。停止本地服务器后，离线首页、投资页及本地数据模拟实测可用。真实 iPhone 的首次文件传入和导入仍由用户完成。

## 发布边界

仅将本目录内已核验的明确文件提交至 `Lin917AI/xuelema`；不批量上传项目父目录。发布保留原有 Pages 配置，不创建收费后端、不购买数据服务、不修改其他仓库。

PWA 使用 `xuelema-v14`：导航分别缓存首页和 `invest/index.html`，不再把所有路径误导回首页；更新仅清理本 App 旧缓存，不清除 IndexedDB，也不删除同源其他项目缓存。

## v3.1 手机布局与推演节奏更新

- 日期框统一48px高度并限制原生内在宽度；600px以内上下排列，避免 iPhone 日期选择框与结束控件重叠。“开始月份”改为“开始时间”。
- 移除三个快捷历史区间，保留任意开始 / 结束月份和至最新数据。
- 触屏和鼠标操作不显示遗留的按钮焦点外圈；键盘导航仍有明确焦点，不以删除无障碍提示解决视觉问题。
- 速度只保留1× / 5×：分别每220ms / 44ms前进一个月。新1×与旧3×相同。
- 弹窗由24个精简为8个：2000年互联网泡沫、2001年九一一、2008年金融危机、2010年欧债危机、2018年贸易摩擦、2020年疫情、2022年战争与通胀紧缩、2025年关税冲击。同一轮危机的后续常规政策不重复打断；2022年统一在6月节点回顾当时已知信息。
- 不改变行情、计算引擎、导入格式和 IndexedDB；升级无需重新导入。保留全部原有阅读内容。
- 本次实际浏览器验收：320px / 390px手机尺寸与1200px桌面日期框均无重叠、页面无横向溢出；关闭事件后按钮无触屏焦点外圈，Tab导航仍显示2px焦点；5×运行2000—2003区间，仅停两个重大节点并正常结算。升级后的旧设备数据仍可读取。未直接操控真实iPhone；手机端采用单列和原生日期控件宽度约束修复。
